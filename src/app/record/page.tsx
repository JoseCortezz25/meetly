import { RecordingWorkspace } from '@/domains/meetings/components/organisms/recording-workspace';

export default function RecordPage() {
  return (
    <main className="relative z-[2] mx-auto max-w-[1000px] px-[34px] pt-[26px] pb-[60px]">
      <RecordingWorkspace />
    </main>
  );
}
